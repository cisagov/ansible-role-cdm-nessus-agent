---
# The name of the S3 object corresponding to the Nessus Agent system
# package
package_object_name: NessusAgent-{{ cdm_nessus_agent_version }}-debian10_{{ 'amd64' if ansible_architecture == 'x86_64' else 'arm64' }}.deb
